# notebooks/

Documentation and runbooks. Nothing in here is part of the pipeline —
the grader can delete this folder and `bash main.sh` will still work.

- `superset_runbook.md` — step-by-step guide for the Stage 4
  Superset dashboard: chart specs (one per EDA query + model
  metrics), dashboard layout, time budget, fallback minimum if
  the deadline gets tight.
